services:
  - type: web
    name: asoiaf-gpt-actions-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run actions:server
    healthCheckPath: /health
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: CANON_ROOT
        value: content/canon
